name: CI-Tests
on: [push]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install and Test
        run: |
          # כניסה ישירה לנתיב שראינו בלוגים שלך
          cd webPro-main/webPro-main/client
          npm install
          npm test